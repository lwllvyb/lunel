// Load all plugins
// This file should be imported once at app startup

// Core plugins (always loaded)
import './core';

// Extra plugins (always loaded, but visibility is configurable)
import './extra';

